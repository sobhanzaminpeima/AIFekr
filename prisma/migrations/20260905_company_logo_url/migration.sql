-- Company logo, uploaded once in Business Doctor and reused on every
-- invoice/contract (CRM) print and payslip/owner-statement (accounting)
-- print output.
ALTER TABLE "Company" ADD COLUMN "logoUrl" TEXT;
